# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: settings.spec.ts >> Settings >> TC01 - Settings - page loads and renders default elements
- Location: tests/settings.spec.ts:13:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Theme toggle' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" getByRole('button', { name: 'Theme toggle' }) with timeout 10000ms
  - waiting for getByRole('button', { name: 'Theme toggle' })

```

```yaml
- main:
  - heading "Settings" [level=1]
  - paragraph: Defaults used when you add a new habit.
  - button "Switch to dark mode": 🌙 Dark
  - button "Logout"
  - link "← Back to habits":
    - /url: /
  - text: Default weekly target
  - paragraph: Pre-fills "Times per week" whenever you add a new habit.
  - combobox "Default weekly target":
    - option "1x / week"
    - option "2x / week"
    - option "3x / week"
    - option "4x / week"
    - option "5x / week"
    - option "6x / week"
    - option "7x / week" [selected]
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Settings', () => {
  4  |   test.setTimeout(60000);
  5  | 
  6  |   // ──────────────────────────────────────────────────────────────────────────
  7  |   // SECTION 1: Settings page load
  8  |   // ──────────────────────────────────────────────────────────────────────────
  9  | 
  10 |   /**
  11 |    * TC01: Settings - page loads and renders default elements
  12 |    */
  13 |   test('TC01 - Settings - page loads and renders default elements', async ({ page }) => {
  14 |     await page.goto('/settings');
  15 |     await expect(page.getByRole('heading', { name: 'Settings' })).toBeVisible();
  16 |     await expect(page.getByText('Defaults used when you add a new habit.')).toBeVisible();
  17 |     await expect(page.getByRole('link', { name: '← Back to habits' })).toBeVisible();
  18 |     await expect(page.getByRole('combobox', { name: 'Default weekly target' })).toBeVisible();
> 19 |     await expect(page.getByRole('button', { name: 'Theme toggle' })).toBeVisible();
     |                                                                      ^ Error: expect(locator).toBeVisible() failed
  20 |     await expect(page.getByRole('button', { name: 'Logout' })).toBeVisible();
  21 |   });
  22 | 
  23 |   // ──────────────────────────────────────────────────────────────────────────
  24 |   // SECTION 2: Settings preference update
  25 |   // ──────────────────────────────────────────────────────────────────────────
  26 | 
  27 |   /**
  28 |    * TC02: Settings - changing default weekly target saves preference and shows saved message
  29 |    */
  30 |   test('TC02 - Settings - changing default weekly target saves preference and shows saved message', async ({ page }) => {
  31 |     await page.goto('/settings');
  32 |     const select = page.getByRole('combobox', { name: 'Default weekly target' });
  33 |     const initialValue = await select.inputValue();
  34 |     const newValue = initialValue === '7' ? '1' : '7';
  35 |     await select.selectOption(newValue);
  36 |     await expect(select).toHaveValue(newValue);
  37 |     await expect(page.getByText('Saved.')).toBeVisible();
  38 |   });
  39 | 
  40 | });
  41 | 
```