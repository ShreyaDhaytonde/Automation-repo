# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: archive.spec.ts >> Archive >> TC01 - Archive - toggles pin state on an archived habit card
- Location: tests/archive.spec.ts:17:7

# Error details

```
Test timeout of 60000ms exceeded.
```

```
Error: locator.fill: Test timeout of 60000ms exceeded.
Call log:
  - waiting for getByRole('textbox', { name: 'New habit name' })

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e3]:
    - generic [ref=e4]:
      - heading "Habit Tracker" [level=1] [ref=e5]
      - paragraph [ref=e6]: Sign in to continue.
    - generic [ref=e7]:
      - generic [ref=e8]: Name
      - textbox "Name" [ref=e9]
    - generic [ref=e10]:
      - generic [ref=e11]: Password
      - textbox "Password" [ref=e12]
    - button "Sign in" [ref=e13]
  - alert [ref=e14]
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | function nameInput(page) {
  4   |   return page.getByRole('textbox', { name: 'New habit name' });
  5   | }
  6   | 
  7   | test.describe('Archive', () => {
  8   |   test.setTimeout(60000);
  9   | 
  10  |   // ──────────────────────────────────────────────────────────────────────────
  11  |   // SECTION 1: Archived habit card pin toggle
  12  |   // ──────────────────────────────────────────────────────────────────────────
  13  | 
  14  |   /**
  15  |    * TC01: Archive - toggles pin state on an archived habit card
  16  |    */
  17  |   test('TC01 - Archive - toggles pin state on an archived habit card', async ({ page }) => {
  18  |     await page.goto('/archive');
  19  |     const nameInput = page.getByRole('textbox', { name: 'New habit name' });
  20  |     const newHabitName = `ArchivePinHabit ${Date.now()}`;
> 21  |     await nameInput.fill(newHabitName);
      |                     ^ Error: locator.fill: Test timeout of 60000ms exceeded.
  22  |     await page.getByRole('button', { name: 'Add habit' }).click();
  23  |     const habitCard = page.getByRole('listitem').filter({ hasText: newHabitName });
  24  |     await habitCard.getByRole('button', { name: `Archive ${newHabitName}` }).click();
  25  |     await expect(habitCard).toHaveCount(0);
  26  |     await page.goto('/archive');
  27  |     const archivedHabitCard = page.getByRole('listitem').filter({ hasText: newHabitName });
  28  |     const pinButton = archivedHabitCard.getByRole('button', { name: new RegExp(`Pin ${newHabitName}|Unpin ${newHabitName}`) });
  29  |     await pinButton.click();
  30  |     const unpinButton = archivedHabitCard.getByRole('button', { name: `Unpin ${newHabitName}` });
  31  |     await expect(unpinButton).toBeVisible();
  32  |     await unpinButton.click();
  33  |     const repinButton = archivedHabitCard.getByRole('button', { name: `Pin ${newHabitName}` });
  34  |     await expect(repinButton).toBeVisible();
  35  |   });
  36  | 
  37  |   // ──────────────────────────────────────────────────────────────────────────
  38  |   // SECTION 2: Archived habit card priority cycle
  39  |   // ──────────────────────────────────────────────────────────────────────────
  40  | 
  41  |   /**
  42  |    * TC02: Archive - cycles priority on an archived habit card
  43  |    */
  44  |   test('TC02 - Archive - cycles priority on an archived habit card', async ({ page }) => {
  45  |     await page.goto('/archive');
  46  |     const nameInput = page.getByRole('textbox', { name: 'New habit name' });
  47  |     const newHabitName = `ArchivePriorityHabit ${Date.now()}`;
  48  |     await nameInput.fill(newHabitName);
  49  |     await page.getByRole('button', { name: 'Add habit' }).click();
  50  |     const habitCard = page.getByRole('listitem').filter({ hasText: newHabitName });
  51  |     await habitCard.getByRole('button', { name: `Archive ${newHabitName}` }).click();
  52  |     await expect(habitCard).toHaveCount(0);
  53  |     await page.goto('/archive');
  54  |     const archivedHabitCard = page.getByRole('listitem').filter({ hasText: newHabitName });
  55  |     const priorityButton = archivedHabitCard.getByRole('button', { name: new RegExp(`Cycle priority for ${newHabitName}, currently (Low|Medium|High)`) });
  56  |     const initialPriority = await priorityButton.textContent();
  57  |     await priorityButton.click();
  58  |     const nextPriorityButton = archivedHabitCard.getByRole('button', { name: new RegExp(`Cycle priority for ${newHabitName}, currently (Low|Medium|High)`) });
  59  |     const nextPriority = await nextPriorityButton.textContent();
  60  |     expect(nextPriority).not.toBe(initialPriority);
  61  |   });
  62  | 
  63  |   // ──────────────────────────────────────────────────────────────────────────
  64  |   // SECTION 3: Archive
  65  |   // ──────────────────────────────────────────────────────────────────────────
  66  | 
  67  |   /**
  68  |    * TC03: HabitCard - toggle pin state on an archived habit card
  69  |    */
  70  |   test('TC03 - HabitCard - toggle pin state on an archived habit card', async ({ page }) => {
  71  |     await page.goto('/');
  72  |     const habitName = `Pin Test Habit ${Date.now()}`;
  73  |     await page.getByRole('textbox', { name: 'New habit name' }).fill(habitName);
  74  |     await page.getByRole('combobox', { name: 'Habit category' }).selectOption({ index: 0 });
  75  |     await page.getByRole('combobox', { name: 'Times per week' }).selectOption('7');
  76  |     await page.getByRole('button', { name: 'Add habit' }).click();
  77  |     const homeHabitCard = page.getByRole('listitem').filter({ hasText: habitName });
  78  |     await expect(homeHabitCard).toBeVisible();
  79  |     await homeHabitCard.getByRole('button', { name: `Archive ${habitName}` }).click();
  80  |     await expect(homeHabitCard).toHaveCount(0);
  81  |     await page.goto('/archive');
  82  |     const habitCard = page.getByRole('listitem').filter({ hasText: habitName });
  83  |     await expect(habitCard).toBeVisible();
  84  |     const pinButton = habitCard.getByRole('button', { name: `Pin ${habitName}` });
  85  |     await pinButton.click();
  86  |     await expect(habitCard.getByRole('button', { name: `Unpin ${habitName}` })).toBeVisible();
  87  |     await habitCard.getByRole('button', { name: `Unpin ${habitName}` }).click();
  88  |     await expect(habitCard.getByRole('button', { name: `Pin ${habitName}` })).toBeVisible();
  89  |   });
  90  | 
  91  |   /**
  92  |    * TC04: HabitCard - cycle priority on an archived habit card
  93  |    */
  94  |   test('TC04 - HabitCard - cycle priority on an archived habit card', async ({ page }) => {
  95  |     await page.goto('/');
  96  |     const habitName = `Priority Test Habit ${Date.now()}`;
  97  |     await page.getByRole('textbox', { name: 'New habit name' }).fill(habitName);
  98  |     await page.getByRole('combobox', { name: 'Habit category' }).selectOption({ index: 0 });
  99  |     await page.getByRole('combobox', { name: 'Times per week' }).selectOption('7');
  100 |     await page.getByRole('button', { name: 'Add habit' }).click();
  101 |     const homeHabitCard = page.getByRole('listitem').filter({ hasText: habitName });
  102 |     await expect(homeHabitCard).toBeVisible();
  103 |     await homeHabitCard.getByRole('button', { name: `Archive ${habitName}` }).click();
  104 |     await expect(homeHabitCard).toHaveCount(0);
  105 |     await page.goto('/archive');
  106 |     const habitCard = page.getByRole('listitem').filter({ hasText: habitName });
  107 |     await expect(habitCard).toBeVisible();
  108 |     const priorityButton = habitCard.getByRole('button', { name: new RegExp(`Cycle priority for ${habitName}, currently (Low|Medium|High)`) });
  109 |     const initialPriority = await priorityButton.textContent();
  110 |     await priorityButton.click();
  111 |     const nextPriority = initialPriority === 'Low' ? 'Medium' : initialPriority === 'Medium' ? 'High' : 'Low';
  112 |     await expect(habitCard.getByRole('button', { name: `Cycle priority for ${habitName}, currently ${nextPriority}` })).toBeVisible();
  113 |   });
  114 | 
  115 |   /**
  116 |    * TC05: Archive page - loads and renders main UI elements
  117 |    */
  118 |   test('TC05 - Archive page - loads and renders main UI elements', async ({ page }) => {
  119 |     await page.goto('/archive');
  120 |     await expect(page.getByRole('heading', { name: 'Archive' })).toBeVisible();
  121 |     await expect(page.getByText("Habits you've archived, out of the main list.")).toBeVisible();
```