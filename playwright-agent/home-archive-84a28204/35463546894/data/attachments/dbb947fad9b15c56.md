# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: home.spec.ts >> Home >> TC02 - Habit form - adds a new habit successfully
- Location: tests/home.spec.ts:34:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('listitem').filter({ hasText: 'Test Habit 1789845342418' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" getByRole('listitem').filter({ hasText: 'Test Habit 1789845342418' }) with timeout 10000ms
  - waiting for getByRole('listitem').filter({ hasText: 'Test Habit 1789845342418' })

```

```yaml
- main:
  - heading "Habit Tracker" [level=1]
  - paragraph: Build small daily habits, one day at a time.
  - link "History":
    - /url: /history
  - link "View stats":
    - /url: /stats
  - link "Archive":
    - /url: /archive
  - button "Switch to dark mode": 🌙 Dark
  - button "Logout"
  - textbox "New habit name":
    - /placeholder: e.g. Drink more water
  - combobox "Habit category":
    - option "General"
    - option "Health" [selected]
    - option "Work"
    - option "Personal"
    - option "Learning"
  - combobox "Times per week":
    - option "1x / week"
    - option "2x / week"
    - option "3x / week" [selected]
    - option "4x / week"
    - option "5x / week"
    - option "6x / week"
    - option "7x / week"
  - textbox "Notes (optional)"
  - button "Add habit" [disabled]
  - text: Search habits by name
  - searchbox "Search habits by name"
  - text: Sort by
  - combobox "Sort habits by":
    - option "Name (A-Z)" [selected]
    - option "Streak (highest first)"
    - option "Category"
    - option "Weekly target (highest first)"
  - text: Filter by category
  - combobox "Filter by category":
    - option "All" [selected]
    - option "General"
    - option "Health"
    - option "Work"
    - option "Personal"
    - option "Learning"
  - checkbox "Show archived"
  - text: Show archived
  - button "Complete all for today (4)"
  - button "Export JSON"
  - button "Export CSV"
  - list:
    - listitem:
      - paragraph: Test Delete 1789845228680
      - text: General
      - status "Test Delete 1789845228680 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Delete 1789845228680 weekly progress"
      - text: 0/7 this week
      - button "Mark done"
      - button "Freeze Test Delete 1789845228680 for today": 🧊 Freeze
      - button "Edit Test Delete 1789845228680": Edit
      - button "Duplicate Test Delete 1789845228680": Duplicate
      - button "Archive Test Delete 1789845228680": Archive
      - button "Delete Test Delete 1789845228680": Remove
    - listitem:
      - paragraph: Test Delete 1789845241070
      - text: General
      - status "Test Delete 1789845241070 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Delete 1789845241070 weekly progress"
      - text: 0/7 this week
      - button "Mark done"
      - button "Freeze Test Delete 1789845241070 for today": 🧊 Freeze
      - button "Edit Test Delete 1789845241070": Edit
      - button "Duplicate Test Delete 1789845241070": Duplicate
      - button "Archive Test Delete 1789845241070": Archive
      - button "Delete Test Delete 1789845241070": Remove
    - listitem:
      - paragraph: Test Edit 1789845290397
      - text: General
      - status "Test Edit 1789845290397 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Edit 1789845290397 weekly progress"
      - text: 0/7 this week
      - button "Mark done"
      - button "Freeze Test Edit 1789845290397 for today": 🧊 Freeze
      - button "Edit Test Edit 1789845290397": Edit
      - button "Duplicate Test Edit 1789845290397": Duplicate
      - button "Archive Test Edit 1789845290397": Archive
      - button "Delete Test Edit 1789845290397": Remove
    - listitem:
      - paragraph: Test Unarchive 1789845253388
      - text: General
      - status "Test Unarchive 1789845253388 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Unarchive 1789845253388 weekly progress"
      - text: 0/7 this week
      - button "Mark done"
      - button "Freeze Test Unarchive 1789845253388 for today": 🧊 Freeze
      - button "Edit Test Unarchive 1789845253388": Edit
      - button "Duplicate Test Unarchive 1789845253388": Duplicate
      - button "Archive Test Unarchive 1789845253388": Archive
      - button "Delete Test Unarchive 1789845253388": Remove
- alert
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('Home', () => {
  4   |   test.setTimeout(60000);
  5   | 
  6   |   // ──────────────────────────────────────────────────────────────────────────
  7   |   // SECTION 1: Home page load
  8   |   // ──────────────────────────────────────────────────────────────────────────
  9   | 
  10  |   /**
  11  |    * TC01: Home page - loads with heading and main controls visible
  12  |    */
  13  |   test('TC01 - Home page - loads with heading and main controls visible', async ({ page }) => {
  14  |     await page.goto("/");
  15  |     await expect(page.getByRole("heading", { name: "Habit Tracker" })).toBeVisible();
  16  |     await expect(page.getByLabel("New habit name")).toBeVisible();
  17  |     await expect(page.getByLabel("Habit category")).toBeVisible();
  18  |     await expect(page.getByLabel("Times per week")).toBeVisible();
  19  |     await expect(page.getByRole("button", { name: "Add habit" })).toBeVisible();
  20  |     await expect(page.getByRole("textbox", { name: "Search habits by name" })).toBeVisible();
  21  |     await expect(page.getByRole("combobox", { name: "Sort habits by" })).toBeVisible();
  22  |     await expect(page.getByLabel("Filter by category")).toBeVisible();
  23  |     await expect(page.getByRole("checkbox", { name: "Show archived" })).toBeVisible();
  24  |     await expect(page.getByRole("button", { name: "Complete all for today" })).toBeVisible();
  25  |   });
  26  | 
  27  |   // ──────────────────────────────────────────────────────────────────────────
  28  |   // SECTION 2: Habit creation
  29  |   // ──────────────────────────────────────────────────────────────────────────
  30  | 
  31  |   /**
  32  |    * TC02: Habit form - adds a new habit successfully
  33  |    */
  34  |   test('TC02 - Habit form - adds a new habit successfully', async ({ page }) => {
  35  |     await page.goto("/");
  36  |     const habitName = `Test Habit ${Date.now()}`;
  37  |     await page.getByLabel("New habit name").fill(habitName);
  38  |     await page.getByLabel("Habit category").selectOption("Health");
  39  |     await page.getByLabel("Times per week").selectOption("3");
  40  |     await page.getByLabel("Notes (optional)").fill("Testing notes");
  41  |     await page.getByRole("button", { name: "Add habit" }).click();
  42  |     const createdHabit = page.getByRole("listitem").filter({ hasText: habitName });
> 43  |     await expect(createdHabit).toBeVisible();
      |                                ^ Error: expect(locator).toBeVisible() failed
  44  |     await expect(createdHabit.getByText("Health")).toBeVisible();
  45  |     await expect(createdHabit.getByText("3x / week")).toBeVisible();
  46  |     await expect(createdHabit.getByText("Testing notes")).toBeVisible();
  47  |   });
  48  | 
  49  |   // ──────────────────────────────────────────────────────────────────────────
  50  |   // SECTION 3: Habit duplication
  51  |   // ──────────────────────────────────────────────────────────────────────────
  52  | 
  53  |   /**
  54  |    * TC03: Habit card - duplicates a habit from the home page
  55  |    */
  56  |   test('TC03 - Habit card - duplicates a habit from the home page', async ({ page }) => {
  57  |     await page.goto("/");
  58  |     const baseName = `Duplicate Habit ${Date.now()}`;
  59  |     await page.getByLabel("New habit name").fill(baseName);
  60  |     await page.getByRole("button", { name: "Add habit" }).click();
  61  |     const originalHabit = page.getByRole("listitem").filter({ hasText: baseName });
  62  |     await expect(originalHabit).toBeVisible();
  63  |     await originalHabit.getByRole("button", { name: `Duplicate ${baseName}` }).click();
  64  |     const duplicateName = `${baseName} (copy)`;
  65  |     const duplicateHabit = page.getByRole("listitem").filter({ hasText: duplicateName });
  66  |     await expect(duplicateHabit).toBeVisible();
  67  |   });
  68  | 
  69  |   // ──────────────────────────────────────────────────────────────────────────
  70  |   // SECTION 4: Filter controls
  71  |   // ──────────────────────────────────────────────────────────────────────────
  72  | 
  73  |   /**
  74  |    * TC04: Filter controls - clear filters button resets filters to defaults
  75  |    */
  76  |   test('TC04 - Filter controls - clear filters button resets filters to defaults', async ({ page }) => {
  77  |     await page.goto("/");
  78  |     await page.getByRole("textbox", { name: "Search habits by name" }).fill("nonexistent-filter-test");
  79  |     await page.getByLabel("Filter by category").selectOption("Health");
  80  |     await page.getByRole("checkbox", { name: "Show archived" }).check();
  81  |     await page.getByRole("combobox", { name: "Sort habits by" }).selectOption("streak");
  82  |     await expect(page.getByRole("button", { name: "Clear filters" })).toBeVisible();
  83  |     await page.getByRole("button", { name: "Clear filters" }).click();
  84  |     await expect(page.getByRole("textbox", { name: "Search habits by name" })).toHaveValue("");
  85  |     await expect(page.getByLabel("Filter by category")).toHaveValue("");
  86  |     await expect(page.getByRole("checkbox", { name: "Show archived" })).not.toBeChecked();
  87  |     await expect(page.getByRole("combobox", { name: "Sort habits by" })).toHaveValue("name");
  88  |   });
  89  | 
  90  |   // ──────────────────────────────────────────────────────────────────────────
  91  |   // SECTION 5: Home
  92  |   // ──────────────────────────────────────────────────────────────────────────
  93  | 
  94  |   /**
  95  |    * TC05: Home - page loads with unconditional elements visible
  96  |    */
  97  |   test('TC05 - Home - page loads with unconditional elements visible', async ({ page }) => {
  98  |     await page.goto('/');
  99  |     await expect(page.getByRole('heading', { name: 'Habit Tracker' })).toBeVisible();
  100 |     await expect(page.getByRole('textbox', { name: 'New habit name' })).toBeVisible();
  101 |     await expect(page.getByLabel('Habit category')).toBeVisible();
  102 |     await expect(page.getByLabel('Times per week')).toBeVisible();
  103 |     await expect(page.getByLabel('Notes (optional)')).toBeVisible();
  104 |     await expect(page.getByRole('button', { name: 'Add habit' })).toBeVisible();
  105 |     await expect(page.getByRole('searchbox', { name: 'Search habits by name' })).toBeVisible();
  106 |     await expect(page.getByRole('combobox', { name: 'Sort habits by' })).toBeVisible();
  107 |     await expect(page.getByRole('combobox', { name: 'Filter by category' })).toBeVisible();
  108 |     await expect(page.getByLabel('Show archived')).toBeVisible();
  109 |     await expect(page.getByRole('link', { name: 'View stats' })).toBeVisible();
  110 |     await expect(page.getByRole('button', { name: 'Complete all for today' })).toBeVisible();
  111 |     await expect(page.getByRole('button', { name: 'Export JSON' })).toBeVisible();
  112 |     await expect(page.getByRole('button', { name: 'Export CSV' })).toBeVisible();
  113 |   });
  114 | 
  115 |   /**
  116 |    * TC06: HabitForm - adds a new habit successfully
  117 |    */
  118 |   test('TC06 - HabitForm - adds a new habit successfully', async ({ page }) => {
  119 |     await page.goto('/');
  120 |     const uniqueName = `Habit ${Date.now()}`;
  121 |     await page.getByRole('textbox', { name: 'New habit name' }).fill(uniqueName);
  122 |     await page.getByLabel('Habit category').selectOption('General');
  123 |     await page.getByLabel('Times per week').selectOption('7');
  124 |     await page.getByRole('textbox', { name: 'Notes (optional)' }).fill('Test notes');
  125 |     await page.getByRole('button', { name: 'Add habit' }).click();
  126 |     const habitItem = page.getByRole('listitem').filter({ hasText: uniqueName });
  127 |     await expect(habitItem).toBeVisible();
  128 |   });
  129 | 
  130 |   /**
  131 |    * TC07: HabitCard - edits a habit\'s name, category, target per week, and notes successfully
  132 |    */
  133 |   test('TC07 - HabitCard - edits a habit\\\'s name, category, target per week, and notes successfully', async ({ page }) => {
  134 |     await page.goto('/');
  135 |     const originalName = `EditTest ${Date.now()}`;
  136 |     const newName = `${originalName} Updated`;
  137 |     await page.getByRole('textbox', { name: 'New habit name' }).fill(originalName);
  138 |     await page.getByRole('button', { name: 'Add habit' }).click();
  139 |     const habitItem = page.getByRole('listitem').filter({ hasText: originalName });
  140 |     await expect(habitItem).toBeVisible();
  141 |     await habitItem.getByRole('button', { name: `Edit ${originalName}` }).click();
  142 |     const nameInput = page.getByLabel(`Edit name for ${originalName}`);
  143 |     await expect(nameInput).toHaveValue(originalName);
```