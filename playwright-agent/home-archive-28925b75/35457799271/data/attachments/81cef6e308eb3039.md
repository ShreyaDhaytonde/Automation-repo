# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: archive.spec.ts >> Archive >> TC05 - Archive page - can delete an archived habit
- Location: tests/archive.spec.ts:66:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('listitem').filter({ hasText: 'Test Delete 1789838750218' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" getByRole('listitem').filter({ hasText: 'Test Delete 1789838750218' }) with timeout 10000ms
  - waiting for getByRole('listitem').filter({ hasText: 'Test Delete 1789838750218' })

```

```yaml
- main:
  - heading "Archive" [level=1]
  - paragraph: Habits you've archived, out of the main list.
  - button "Switch to dark mode": 🌙 Dark
  - button "Logout"
  - link "← Back to habits":
    - /url: /
  - list:
    - listitem:
      - paragraph: Test Delete 1789838318091
      - text: General
      - status "Test Delete 1789838318091 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Delete 1789838318091 weekly progress"
      - text: 0/7 this week
      - button "Mark done" [disabled]
      - button "Freeze Test Delete 1789838318091 for today" [disabled]: 🧊 Freeze
      - button "Edit Test Delete 1789838318091": Edit
      - button "Duplicate Test Delete 1789838318091": Duplicate
      - button "Unarchive Test Delete 1789838318091": Unarchive
      - button "Delete Test Delete 1789838318091": Remove
    - listitem:
      - paragraph: Test Delete 1789838330717
      - text: General
      - status "Test Delete 1789838330717 is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Delete 1789838330717 weekly progress"
      - text: 0/7 this week
      - button "Mark done" [disabled]
      - button "Freeze Test Delete 1789838330717 for today" [disabled]: 🧊 Freeze
      - button "Edit Test Delete 1789838330717": Edit
      - button "Duplicate Test Delete 1789838330717": Duplicate
      - button "Unarchive Test Delete 1789838330717": Unarchive
      - button "Delete Test Delete 1789838330717": Remove
    - listitem:
      - paragraph: Test Edit 1789838344372 updated
      - text: General
      - status "Test Edit 1789838344372 updated is at risk of missing its weekly goal": ⏰ Due today
      - paragraph: Start your streak today!
      - progressbar "Test Edit 1789838344372 updated weekly progress"
      - text: 0/7 this week
      - button "Mark done" [disabled]
      - button "Freeze Test Edit 1789838344372 updated for today" [disabled]: 🧊 Freeze
      - button "Edit Test Edit 1789838344372 updated": Edit
      - button "Duplicate Test Edit 1789838344372 updated": Duplicate
      - button "Unarchive Test Edit 1789838344372 updated": Unarchive
      - button "Delete Test Edit 1789838344372 updated": Remove
- alert
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('Archive', () => {
  4   |   test.setTimeout(60000);
  5   | 
  6   |   // ──────────────────────────────────────────────────────────────────────────
  7   |   // SECTION 1: HabitCard duplicate behavior
  8   |   // ──────────────────────────────────────────────────────────────────────────
  9   | 
  10  |   /**
  11  |    * TC01: HabitCard - duplicate archived habit creates active copy (not shown in archive)
  12  |    */
  13  |   test('TC01 - HabitCard - duplicate archived habit creates active copy (not shown in archive)', async ({ page }) => {
  14  |     await page.goto('/archive');
  15  |     const archivedHabitCard = page.getByRole('listitem').first();
  16  |     const habitName = await archivedHabitCard.locator('p:visible').first().innerText();
  17  |     const duplicateButton = archivedHabitCard.getByRole('button', { name: `Duplicate ${habitName}` });
  18  |     await duplicateButton.click();
  19  |     const duplicatedHabit = page.getByRole('listitem').filter({ hasText: `${habitName} (copy)` });
  20  |     await expect(duplicatedHabit).toHaveCount(0);
  21  |   });
  22  | 
  23  |   // ──────────────────────────────────────────────────────────────────────────
  24  |   // SECTION 2: Archive
  25  |   // ──────────────────────────────────────────────────────────────────────────
  26  | 
  27  |   /**
  28  |    * TC02: Archive page - loads and renders unconditional elements
  29  |    */
  30  |   test('TC02 - Archive page - loads and renders unconditional elements', async ({ page }) => {
  31  |     await page.goto("/archive");
  32  |     await expect(page.getByRole("heading", { name: "Archive" })).toBeVisible();
  33  |     await expect(page.getByRole("link", { name: "← Back to habits" })).toBeVisible();
  34  |     await expect(page.getByRole("button", { name: /Edit / })).toBeVisible();
  35  |   });
  36  | 
  37  |   /**
  38  |    * TC03: HabitCard in Archive - duplicates an archived habit, which does not appear in Archive but is presumably added to Home
  39  |    */
  40  |   test('TC03 - HabitCard in Archive - duplicates an archived habit, which does not appear in Archive but is presumably added to Home', async ({ page }) => {
  41  |     await page.goto("/archive");
  42  |     const archivedHabitEditButton = page.getByRole("button", { name: /Edit / }).first();
  43  |     await archivedHabitEditButton.click();
  44  |     const archivedHabitNameLocator = page.getByRole("textbox", { name: /Edit name for / }).first();
  45  |     const archivedHabitName = await archivedHabitNameLocator.inputValue();
  46  |     const duplicateButton = page.getByRole("button", { name: `Duplicate ${archivedHabitName}` });
  47  |     await duplicateButton.click();
  48  |     await expect(page.locator(`text=${archivedHabitName} (copy)`)).toHaveCount(0);
  49  |     await expect(page.getByText("Could not duplicate that habit — try again.")).toHaveCount(0);
  50  |   });
  51  | 
  52  |   /**
  53  |    * TC04: Archive page - loads and shows static elements
  54  |    */
  55  |   test('TC04 - Archive page - loads and shows static elements', async ({ page }) => {
  56  |     await page.goto('/archive');
  57  |     await expect(page.getByRole('heading', { name: 'Archive' })).toBeVisible();
  58  |     await expect(page.getByRole('link', { name: '← Back to habits' })).toBeVisible();
  59  |     await expect(page.getByRole('button', { name: 'Logout' })).toBeVisible();
  60  |     await expect(page.getByRole('button', { name: /switch to dark mode/i })).toBeVisible();
  61  |   });
  62  | 
  63  |   /**
  64  |    * TC05: Archive page - can delete an archived habit
  65  |    */
  66  |   test('TC05 - Archive page - can delete an archived habit', async ({ page }) => {
  67  |     await page.goto('/');
  68  |     const uniqueName = `Test Delete ${Date.now()}`;
  69  |     await page.getByLabel('New habit name').fill(uniqueName);
  70  |     await page.getByLabel('Habit category').selectOption({ index: 0 });
  71  |     await page.getByLabel('Times per week').selectOption('7');
  72  |     await page.getByLabel('Notes (optional)').fill('');
  73  |     await page.getByRole('button', { name: 'Add habit' }).click();
  74  |     const habitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
  75  |     await expect(habitCard).toBeVisible();
  76  |     await habitCard.getByRole('button', { name: `Archive` }).click();
  77  |     await page.goto('/archive');
  78  |     const archivedHabitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
> 79  |     await expect(archivedHabitCard).toBeVisible();
      |                                     ^ Error: expect(locator).toBeVisible() failed
  80  |     await archivedHabitCard.getByRole('button', { name: `Delete ${uniqueName}`, exact: true }).click();
  81  |     await page.waitForTimeout(500);
  82  |     await expect(archivedHabitCard).toHaveCount(0);
  83  |   });
  84  | 
  85  |   /**
  86  |    * TC06: Archive page - can unarchive an archived habit
  87  |    */
  88  |   test('TC06 - Archive page - can unarchive an archived habit', async ({ page }) => {
  89  |     await page.goto('/');
  90  |     const uniqueName = `Test Unarchive ${Date.now()}`;
  91  |     await page.getByLabel('New habit name').fill(uniqueName);
  92  |     await page.getByLabel('Habit category').selectOption({ index: 0 });
  93  |     await page.getByLabel('Times per week').selectOption('7');
  94  |     await page.getByLabel('Notes (optional)').fill('');
  95  |     await page.getByRole('button', { name: 'Add habit' }).click();
  96  |     const habitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
  97  |     await expect(habitCard).toBeVisible();
  98  |     await habitCard.getByRole('button', { name: `Archive ${uniqueName}`, exact: true }).click();
  99  |     await page.goto('/archive');
  100 |     const archivedHabitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
  101 |     await expect(archivedHabitCard).toBeVisible();
  102 |     await archivedHabitCard.getByRole('button', { name: `Unarchive ${uniqueName}`, exact: true }).click();
  103 |     await expect(page.getByRole('listitem').filter({ hasText: uniqueName })).toHaveCount(0);
  104 |   });
  105 | 
  106 |   /**
  107 |    * TC07: Archive page - can edit an archived habit\\\\\\\'s details
  108 |    */
  109 |   test('TC07 - Archive page - can edit an archived habit\\\\\\\\\\\\\\\'s details', async ({ page }) => {
  110 |     await page.goto('/');
  111 |     const uniqueName = `Test Edit ${Date.now()}`;
  112 |     await page.getByLabel('New habit name').fill(uniqueName);
  113 |     await page.getByLabel('Habit category').selectOption({ index: 0 });
  114 |     await page.getByLabel('Times per week').selectOption('7');
  115 |     await page.getByLabel('Notes (optional)').fill('');
  116 |     await page.getByRole('button', { name: 'Add habit' }).click();
  117 |     const habitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
  118 |     await expect(habitCard).toBeVisible();
  119 |     await habitCard.getByRole('button', { name: `Archive` }).click();
  120 |     await page.goto('/archive');
  121 |     const archivedHabitCard = page.getByRole('listitem').filter({ hasText: uniqueName });
  122 |     await expect(archivedHabitCard).toBeVisible();
  123 |     await archivedHabitCard.getByRole('button', { name: `Edit ${uniqueName}`, exact: true }).click();
  124 |     const nameInput = page.getByLabel(`Edit name for ${uniqueName}`);
  125 |     await expect(nameInput).toHaveValue(uniqueName);
  126 |     await nameInput.fill(`${uniqueName} updated`);
  127 |     const saveButton = page.getByRole('button', { name: 'Save', exact: true });
  128 |     await saveButton.click();
  129 |     const updatedHabitCard = page.getByRole('listitem').filter({ hasText: `${uniqueName} updated` });
  130 |     await expect(updatedHabitCard).toBeVisible();
  131 |   });
  132 | 
  133 |   /**
  134 |    * TC08: Archive page - can navigate back to home page
  135 |    */
  136 |   test('TC08 - Archive page - can navigate back to home page', async ({ page }) => {
  137 |     await page.goto('/archive');
  138 |     await page.getByRole('link', { name: '← Back to habits' }).click();
  139 |     await expect(page).toHaveURL('/');
  140 |     await expect(page.getByRole('heading', { name: 'Habit Tracker' })).toBeVisible();
  141 |   });
  142 | 
  143 |   /**
  144 |    * TC09: Archive page - error message appears when loading fails (non-deterministic)
  145 |    */
  146 |   test('TC09 - Archive page - error message appears when loading fails (non-deterministic)', async ({ page }) => {
  147 |     await page.goto('/archive');
  148 |     await expect(page.locator('text=Could not load archived habits. Is the API running?')).toBeHidden();
  149 |   });
  150 | 
  151 |   /**
  152 |    * TC10: Archive page - empty state message is shown when no habits are archived (not reachable without backend control)
  153 |    */
  154 |   test('TC10 - Archive page - empty state message is shown when no habits are archived (not reachable without backend control)', async ({ page }) => {
  155 |     await page.goto('/archive');
  156 |     await expect(page.locator('text=No archived habits — anything you archive from the home page shows up here.')).toBeHidden();
  157 |   });
  158 | 
  159 | });
  160 | 
```